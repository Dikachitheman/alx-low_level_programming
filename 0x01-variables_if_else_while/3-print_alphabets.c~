#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - stuff
 * Return: 0
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	char c = 'A';
	while (c <= 'Z')
	{
		putchar(c);
		c++;
	}
	return (0);
}
