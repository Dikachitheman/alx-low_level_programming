#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - stuff
 * Return: 0
 */

int main(void)
{
	char ch = 'a';
	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}

	charr ch1= 'A';
	while (ch1 <= 'Z')
	{
		putchar(ch1);
		ch++;
	}
	return (0);
}
