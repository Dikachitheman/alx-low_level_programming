#include <stdio.h>

/**
 * main - stuff
 *Return: 0
 */

int main(void)
{
	int c;

	for (c = 48; c < 58; c++)
	{
		putchar(c);

		if (c < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
