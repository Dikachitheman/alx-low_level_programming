#include "main.h"
#include <stdio.h>
/**
 * main - stuff
 * Description: stuff
 * Return: stuff
 */

int sum
int i;

for (i = 0; i < 1024; i++)
{
	if ((i % 3 == 0) || (i % 5 == 0))
	{
		sum = sum + 1;
	}
}
printf("%d\n", sum);
return (0);
}
